# AGENTS

- Golden TTB regression fixture uses a text-only format (`page|y|text`, `#` comments allowed); keep it sanitized and update `ttb_statement_sample_golden.json` when parser changes are intentional.
